[project]
name = "amplifier-module-hooks-deepwiki-trigger"
version = "0.2.1"
description = "Hook that detects GitHub URLs and library questions, injecting reminders to use deepwiki-expert"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
